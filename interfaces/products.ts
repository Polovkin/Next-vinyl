export interface ProductsArray {
  productsArr: [name: string, email: string, phone: string, message: string, status: string]
}

export interface Products {
  name: string
  email: string
  phone: string
  message: string
  status: string
}
